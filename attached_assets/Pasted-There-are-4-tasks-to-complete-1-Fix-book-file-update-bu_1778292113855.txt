There are 4 tasks to complete:
1. Fix book file update bug
When a user edits a book and uploads a new file (PDF/EPUB), the new file is uploaded successfully and saved to the database, but the download still serves the old file. The issue is that the compiled dist/ of @workspace/api-zod doesn't include the fileObjectPath and fileFormat fields that were added to UpdateBookBody. Make sure UpdateBookBody in lib/api-zod/src/generated/api.ts includes fileObjectPath: zod.string().optional() and fileFormat: zod.enum(["pdf", "epub"]).optional(), rebuild the package, and ensure the PATCH /api/books/:bookId endpoint in artifacts/api-server/src/routes/books.ts saves both fields. Also make sure the Workflows command for the API server rebuilds api-zod before starting: pnpm --filter @workspace/api-zod run build && pnpm --filter @workspace/api-server run dev. After fixing, verify by editing a book, uploading a new file, and confirming the download serves the new file.
2. Full profile customization
Enhance the profile page (artifacts/bookshelf/src/pages/me.tsx) so users can edit: display name, username, bio, avatar image, and add a banner/cover image for their profile. Also show a "joined date" and book count. Make the public profile page (profile.tsx) reflect all these new fields too.
3. Multi-language support
Add internationalization (i18n) to the app using i18next and react-i18next. Support at least these languages: English, Spanish, German, Russian, and French. Add a language selector in the navigation bar or settings. Translate all UI text in the app including navigation, buttons, forms, and messages.
4. Auto-import public domain books from Project Gutenberg
Create a script at scripts/import-gutenberg.ts that fetches books from the Project Gutenberg API (https://gutenberg.dita.digital/books or https://gutendex.com/books), imports them into the database as public books with title, author, description, cover image URL, and a link to the download file. Run the script to import at least 50 popular books automatically. These books should appear in the Browse page for all users to read and download.