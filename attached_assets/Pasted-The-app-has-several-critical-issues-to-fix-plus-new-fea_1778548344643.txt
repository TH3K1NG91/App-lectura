The app has several critical issues to fix, plus new features to add:
CRITICAL FIXES:
1. Database sync issue — The app has two separate databases (development and production) that are out of sync. Make sure the app always uses the production DATABASE_URL from secrets in both development and production environments. After connecting, verify that both environments see the same data.
2. App broken after GitHub sync — The Bookshelf/Lumina frontend artifact is failing to run and the API server may also be broken. Debug and fix both so the app runs correctly. Check that all workflows are running and that the frontend can communicate with the backend.
3. Profile update failing — When a user tries to save profile changes (display name, username, bio), it shows "Failed to update profile". Fix the PATCH /api/users/me endpoint so profile updates work correctly and are saved to the database.
4. Books not showing — Books that were previously imported are not visible in the app. After fixing the database connection, make sure all existing books appear correctly in the Browse/Explorar page.
NEW FEATURES:
5. Import English books — Run the Gutenberg import script to populate the production database with at least 100 popular English-language public domain books with cover images, descriptions, and correct genre tags.
6. Import Spanish books — Run the Gutenberg import script again using the ?languages=es filter from the Gutendex API (https://gutendex.com/books/?languages=es) to import at least 100 Spanish-language public domain books with cover images, descriptions, and genre tags. These should appear alongside the English books in the Browse page.
7. Fix Feed page — The Feed page shows "En tendencia" but the carousel is empty and books don't load. Fix it so trending books actually appear in the carousel. Also fix the "Configurar preferencias" button so the genre quiz works and saves preferences correctly to the user profile.
After all fixes, restart both servers, verify the app loads correctly, and confirm books are visible in both the development preview and the published production URL.